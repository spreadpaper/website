---
import Icon from '../Icon.astro'
---

<!-- The three kinds of wallpaper, as one switchable panel rather than three stacked rows. -->
<!-- `data-clock-stops` lists the hours the Dynamic schedule below names, so the day loop
     starts at the reader's own hour instead of always at sunrise. Keep it in step with the
     times in that readout: the script only counts positions, it never reads the markup. -->
<section id="types" class="cd-section bg-cd-canvas" aria-labelledby="types-title" data-tabs data-clock-stops="6,12,18,23">
    <style>
        /* Without script the three panels read as a stacked list, so they need the air
           between them and their own kind labels. `data-tabs-ready`, set on the section
           once the tab script wires it, folds that list into the tablist instead. */
        #types [data-types-panel] + [data-types-panel] { margin-top: 4.5rem; }
        #types[data-tabs-ready] [data-types-panel] + [data-types-panel] { margin-top: 0; }
        #types[data-tabs-ready] .types-kind { display: none; }

        /* The three panels stack in one grid cell, so the container always reserves the
           tallest of them and choosing a tab never moves the page under the reader.
           Reserving the tallest rather than naming a height is what holds at every width:
           the rigs have fixed but different aspects, and the captions and readouts rewrap,
           so which panel is tallest is not a constant worth guessing at.

           A closed panel therefore has to keep its box, which rules out the `hidden`
           attribute. Tailwind's preflight sets `[hidden] { display: none !important }`
           inside `@layer base`, and importance is settled before both layer order and
           specificity, so no unlayered rule here can outrank it and the panel would leave
           the grid. The tab script marks a closed panel `inert` instead, which keeps the
           box while still taking the panel out of the tab order and the accessibility
           tree, which is the part `hidden` was doing the work for. */
        #types[data-tabs-ready] .types-panels { display: grid; }

        #types[data-tabs-ready] [data-types-panel] {
            grid-area: 1 / 1;
            transition: opacity 200ms ease-out, transform 200ms ease-out;
        }

        #types[data-tabs-ready] [data-types-panel][inert] {
            visibility: hidden;
            opacity: 0;
            transform: translateY(6px);
        }

        /* The day starts at the reader's own hour rather than always at sunrise, so someone
           opening the page in the evening sees the evening photograph first. `--clock-phase`
           is a unitless fraction of one cycle, set by the tab script off the wall clock, and
           it shifts the photographs and the stop bars by the same amount so the two cannot
           drift. It defaults to 0, which is the loop starting at 06:00 as before, so this
           costs nothing while the script does not set it. */
        #types {
            --clock-offset: calc(var(--rig-day-cycle, 20s) * var(--clock-phase, 0) * -1);
        }

        #types .rig-day { animation-delay: var(--clock-offset); }

        /* The bar over a stop in a readout, lit while the photograph it names is the one on
           the wall. Both readouts run off the rig's own cycle properties rather than a
           duration of their own, so a rig repaced from anywhere carries its times with it.
           Lit is the resting state, so with motion off every stop reads as part of the
           schedule rather than every stop reading as switched off. */
        #types .types-bar {
            animation: types-stop var(--rig-day-cycle, 20s) linear infinite;
            animation-delay: var(--clock-offset);
        }

        @keyframes types-stop {
            0%, 20% { opacity: 1; }
            25%, 95% { opacity: 0.24; }
            100% { opacity: 1; }
        }

        /* Negative delays put the later stops of the day in phase from the first frame. */
        #types .types-bar-2 { animation-delay: calc(var(--rig-day-cycle, 20s) * -0.75 + var(--clock-offset)); }
        #types .types-bar-3 { animation-delay: calc(var(--rig-day-cycle, 20s) * -0.5 + var(--clock-offset)); }
        #types .types-bar-4 { animation-delay: calc(var(--rig-day-cycle, 20s) * -0.25 + var(--clock-offset)); }

        /* The appearance pair is one cycle split unevenly, so its two stops take the rig
           crossfade's own timings rather than a delay off one keyframe. */
        #types .types-bar-light { animation: types-stop-light var(--rig-cycle, 12s) ease-in-out infinite; }
        #types .types-bar-dark { animation: types-stop-dark var(--rig-cycle, 12s) ease-in-out infinite; }

        @keyframes types-stop-light {
            0%, 40% { opacity: 1; }
            50%, 90% { opacity: 0.24; }
            100% { opacity: 1; }
        }

        @keyframes types-stop-dark {
            0%, 40% { opacity: 0.24; }
            50%, 90% { opacity: 1; }
            100% { opacity: 0.24; }
        }

        @media (prefers-reduced-motion: reduce) {
            #types[data-tabs-ready] [data-types-panel] { transition: none; }

            #types .types-bar,
            #types .types-bar-light,
            #types .types-bar-dark {
                animation: none !important;
            }
        }
    </style>

    <div class="cd-shell">
        <div class="grid grid-cols-12 gap-x-6 gap-y-10" data-reveal>
            <header class="col-span-12 lg:col-span-6">
                <p class="cd-eyebrow">Three kinds</p>
                <h2 id="types-title" class="mt-3 text-3xl sm:text-4xl lg:text-5xl font-bold tracking-[-0.025em] leading-[1.05] text-cd-text text-balance">Change with the day, with the light, or not at all</h2>
                <p class="mt-5 text-lg sm:text-xl leading-relaxed text-cd-text-secondary max-w-[46ch] text-pretty">Every wallpaper SpreadPaper makes is one photograph laid across the displays you actually have, however many that is. What changes between the three kinds is how many photographs you hand it, and what decides when they change.</p>
                <p class="mt-4 text-base leading-relaxed text-cd-text max-w-[52ch] text-pretty">Two of the three ask for no second display at all. A Light and Dark pair and a day on a schedule are worth having on a single screen, and a laptop on its own is a perfectly ordinary way to run this.</p>
            </header>

            <!-- Hidden until the script wires it, so a page without JavaScript never shows
                 three buttons that do nothing.

                 Each tab sets its resting left edge with `border-l-transparent` rather than
                 `border-transparent`, and the distinction is load bearing. `divide-cd-border`
                 colours the dividers through `:where(& > :not(:last-child))`, which carries
                 no specificity, so a child setting `border-color` on every side overrides it
                 and silently erases every divider in the list while the markup still reads
                 correctly. Colouring only the edge the tab actually uses leaves the dividers
                 alone. -->
            <div class="col-span-12 lg:col-span-5 lg:col-start-8 lg:self-end" data-tabs-list hidden>
                <div role="tablist" aria-orientation="vertical" aria-label="Kinds of wallpaper" class="divide-y divide-cd-border border-y border-cd-border">
                    <button type="button" role="tab" id="types-tab-dynamic" aria-controls="types-panel-dynamic" aria-selected="true" class="flex w-full items-center gap-4 border-l-2 border-l-transparent py-4 pl-4 pr-3 text-left transition-colors duration-150 ease-out hover:bg-cd-bg-secondary aria-selected:border-l-cd-dynamic aria-selected:bg-cd-bg-secondary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent">
                        <Icon name="clock" class="size-6 shrink-0 text-cd-dynamic" />
                        <span>
                            <span class="block text-base font-semibold text-cd-text">Dynamic</span>
                            <span class="block text-sm text-cd-text-secondary">Up to 16 images</span>
                        </span>
                    </button>
                    <button type="button" role="tab" id="types-tab-appearance" aria-controls="types-panel-appearance" aria-selected="false" class="flex w-full items-center gap-4 border-l-2 border-l-transparent py-4 pl-4 pr-3 text-left transition-colors duration-150 ease-out hover:bg-cd-bg-secondary aria-selected:border-l-cd-appearance aria-selected:bg-cd-bg-secondary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent">
                        <Icon name="circle-half" class="size-6 shrink-0 text-cd-appearance" />
                        <span>
                            <span class="block text-base font-semibold text-cd-text">Light &amp; Dark</span>
                            <span class="block text-sm text-cd-text-secondary">Two images</span>
                        </span>
                    </button>
                    <button type="button" role="tab" id="types-tab-static" aria-controls="types-panel-static" aria-selected="false" class="flex w-full items-center gap-4 border-l-2 border-l-transparent py-4 pl-4 pr-3 text-left transition-colors duration-150 ease-out hover:bg-cd-bg-secondary aria-selected:border-l-cd-border-strong aria-selected:bg-cd-bg-secondary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent">
                        <Icon name="image" class="size-6 shrink-0 text-cd-text-secondary" />
                        <span>
                            <span class="block text-base font-semibold text-cd-text">Static</span>
                            <span class="block text-sm text-cd-text-secondary">One image</span>
                        </span>
                    </button>
                </div>
            </div>
        </div>

        <div class="types-panels mt-14 lg:mt-20" data-reveal>
            <div id="types-panel-dynamic" data-types-panel class="focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent">
                <div class="grid grid-cols-12 gap-x-6 gap-y-8 lg:items-center">
                    <div class="col-span-12 lg:col-span-8 lg:col-start-1 lg:row-start-1">
                        <figure>
                            <svg class="rig rig-portrait-trio" style="--rig-glow-color: rgb(245 165 36 / 0.14)" viewBox="0 0 767 391" role="img" aria-label="Green mountains at sunrise carried across a portrait monitor, a landscape monitor and another portrait monitor, running through one day." focusable="false">
                              <defs>
                                <clipPath id="rig-types-dynamic">
                                  <rect x="0" y="0" width="200" height="355" rx="10"/>
                                  <rect x="206" y="155" width="355" height="200" rx="10"/>
                                  <rect x="567" y="0" width="200" height="355" rx="10"/>
                                </clipPath>
                              </defs>
                              <ellipse class="rig-glow" cx="383.5" cy="387" rx="337" ry="18"/>
                              <g class="rig-stand">
                                <rect x="86" y="355" width="28" height="28"/><rect x="45" y="383" width="110" height="8" rx="4"/>
                                <rect x="369.5" y="355" width="28" height="28"/><rect x="328.5" y="383" width="110" height="8" rx="4"/>
                                <rect x="653" y="355" width="28" height="28"/><rect x="612" y="383" width="110" height="8" rx="4"/>
                              </g>
                              <g clip-path="url(#rig-types-dynamic)">
                                <image class="rig-photo" href="/photos/1200/hero-day-1.jpg" x="0" y="0" width="767" height="355" preserveAspectRatio="xMidYMid slice"/>
                                <image class="rig-photo rig-day rig-day-2" href="/photos/1200/hero-day-2.jpg" x="0" y="0" width="767" height="355" preserveAspectRatio="xMidYMid slice"/>
                                <image class="rig-photo rig-day rig-day-3" href="/photos/1200/hero-day-3.jpg" x="0" y="0" width="767" height="355" preserveAspectRatio="xMidYMid slice"/>
                                <image class="rig-photo rig-day rig-day-4" href="/photos/1200/hero-day-4.jpg" x="0" y="0" width="767" height="355" preserveAspectRatio="xMidYMid slice"/>
                                <image class="rig-photo rig-day rig-day-wrap" href="/photos/1200/hero-day-1.jpg" x="0" y="0" width="767" height="355" preserveAspectRatio="xMidYMid slice"/>
                                <rect class="rig-frame" x="0" y="0" width="200" height="355" rx="10"/>
                                <rect class="rig-frame" x="206" y="155" width="355" height="200" rx="10"/>
                                <rect class="rig-frame" x="567" y="0" width="200" height="355" rx="10"/>
                              </g>
                            </svg>
                            <figcaption class="mt-4 text-sm text-cd-text-secondary">Photographs by <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@slohmann">Simon Lohmann</a>, <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@antoskli">Antony Sklivagkos</a>, <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@mparzuchowski">Michał Parzuchowski</a> and <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@vorosbenisop">Benjamin Voros</a> on Unsplash.</figcaption>
                        </figure>
                        <ol role="list" class="mt-6 grid grid-cols-2 gap-x-4 gap-y-5 sm:grid-cols-4">
                            <li>
                                <span class="types-bar block h-0.5 rounded-full bg-cd-dynamic" aria-hidden="true"></span>
                                <span class="mt-2.5 block text-sm font-semibold tabular-nums text-cd-dynamic">06:00</span>
                                <span class="mt-0.5 block text-sm text-cd-text-secondary">Sunrise</span>
                            </li>
                            <li>
                                <span class="types-bar types-bar-2 block h-0.5 rounded-full bg-cd-dynamic" aria-hidden="true"></span>
                                <span class="mt-2.5 block text-sm font-semibold tabular-nums text-cd-dynamic">12:00</span>
                                <span class="mt-0.5 block text-sm text-cd-text-secondary">Midday</span>
                            </li>
                            <li>
                                <span class="types-bar types-bar-3 block h-0.5 rounded-full bg-cd-dynamic" aria-hidden="true"></span>
                                <span class="mt-2.5 block text-sm font-semibold tabular-nums text-cd-dynamic">18:00</span>
                                <span class="mt-0.5 block text-sm text-cd-text-secondary">Evening light</span>
                            </li>
                            <li>
                                <span class="types-bar types-bar-4 block h-0.5 rounded-full bg-cd-dynamic" aria-hidden="true"></span>
                                <span class="mt-2.5 block text-sm font-semibold tabular-nums text-cd-dynamic">23:00</span>
                                <span class="mt-0.5 block text-sm text-cd-text-secondary">The Milky Way</span>
                            </li>
                        </ol>
                    </div>
                    <div class="col-span-12 lg:col-span-4 lg:col-start-9 lg:row-start-1">
                        <p class="types-kind flex items-center gap-2.5 text-sm font-semibold text-cd-dynamic">
                            <Icon name="clock" class="size-5 shrink-0" />Dynamic, up to 16 images
                        </p>
                        <h3 class="mt-3 text-xl font-semibold tracking-[-0.01em] text-cd-text">A whole day on a schedule</h3>
                        <p class="mt-4 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Up to 16 photographs, each with a start time you set on the clock. SpreadPaper writes them into a HEIC dynamic desktop carrying the time metadata macOS reads, so every screen turns over at the same moment.</p>
                    </div>
                </div>
            </div>
            <div id="types-panel-appearance" data-types-panel class="focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent">
                <div class="grid grid-cols-12 gap-x-6 gap-y-8 lg:items-center">
                    <div class="col-span-12 lg:col-span-8 lg:col-start-1 lg:row-start-1">
                        <figure>
                            <svg class="rig rig-laptop" style="--rig-glow-color: rgb(124 124 255 / 0.14)" viewBox="0 0 550 236" role="img" aria-label="A beach at sunset carried across a monitor and the laptop beside it, fading between the light image and the dark one." focusable="false">
                              <defs>
                                <clipPath id="rig-types-appearance">
                                  <rect x="0" y="0" width="355" height="200" rx="10"/>
                                  <rect x="361" y="101" width="181" height="117" rx="8"/>
                                </clipPath>
                              </defs>
                              <ellipse class="rig-glow" cx="275" cy="232" rx="242" ry="18"/>
                              <g class="rig-stand">
                                <rect x="163.5" y="200" width="28" height="28"/><rect x="122.5" y="228" width="110" height="8" rx="4"/>
                                <rect x="357" y="230" width="189" height="6" rx="3"/>
                              </g>
                              <rect class="rig-chin" x="361" y="209" width="181" height="21" rx="3"/>
                              <g clip-path="url(#rig-types-appearance)">
                                <image class="rig-photo" href="/photos/hero-beach.jpg" x="0" y="0" width="550" height="218" preserveAspectRatio="xMidYMid slice"/>
                                <image class="rig-photo rig-photo-fade" href="/photos/1200/hero-beach-night.jpg" x="0" y="0" width="550" height="218" preserveAspectRatio="xMidYMid slice"/>
                                <rect class="rig-frame" x="0" y="0" width="355" height="200" rx="10"/>
                                <rect class="rig-frame" x="361" y="101" width="181" height="117" rx="8"/>
                              </g>
                            </svg>
                            <figcaption class="mt-4 text-sm text-cd-text-secondary">The laptop sits lower than the monitor, so it shows the lower part of the picture, which is what macOS does with a real arrangement. Light half by <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@gkumar2175">Ganapathy Kumar</a>, dark half by <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@m_hampi">Michaela</a>, both on Unsplash.</figcaption>
                        </figure>
                        <ul role="list" class="mt-6 grid grid-cols-2 gap-4">
                            <li>
                                <span class="types-bar-light block h-0.5 rounded-full bg-cd-appearance" aria-hidden="true"></span>
                                <span class="mt-2.5 block text-sm font-semibold text-cd-appearance">Light</span>
                                <span class="mt-0.5 block text-sm text-cd-text-secondary">The beach</span>
                            </li>
                            <li>
                                <span class="types-bar-dark block h-0.5 rounded-full bg-cd-appearance" aria-hidden="true"></span>
                                <span class="mt-2.5 block text-sm font-semibold text-cd-appearance">Dark</span>
                                <span class="mt-0.5 block text-sm text-cd-text-secondary">The palms</span>
                            </li>
                        </ul>
                    </div>
                    <div class="col-span-12 lg:col-span-4 lg:col-start-9 lg:row-start-1">
                        <p class="types-kind flex items-center gap-2.5 text-sm font-semibold text-cd-appearance">
                            <Icon name="circle-half" class="size-5 shrink-0" />Light &amp; Dark, two images
                        </p>
                        <h3 class="mt-3 text-xl font-semibold tracking-[-0.01em] text-cd-text">A pair that follows the appearance</h3>
                        <p class="mt-4 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Hand SpreadPaper two photographs, one for light appearance and one for dark. Switch your Mac yourself and the wallpaper follows on every display at once. Set Appearance to Auto and macOS makes the switch on the same schedule Night Shift runs on.</p>
                    </div>
                </div>
            </div>

            <div id="types-panel-static" data-types-panel class="focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent">
                <div class="grid grid-cols-12 gap-x-6 gap-y-8 lg:items-center">
                    <figure class="col-span-12 lg:col-span-8 lg:col-start-1 lg:row-start-1">
                        <svg class="rig rig-dual" viewBox="0 0 716 236" role="img" aria-label="A beach at sunset in Maui, one photograph carried across two monitors side by side." focusable="false">
                          <defs>
                            <clipPath id="rig-types-static">
                              <rect x="0" y="0" width="355" height="200" rx="10"/>
                              <rect x="361" y="0" width="355" height="200" rx="10"/>
                            </clipPath>
                          </defs>
                          <ellipse class="rig-glow" cx="358" cy="232" rx="315" ry="18"/>
                          <g class="rig-stand">
                            <rect x="163.5" y="200" width="28" height="28"/><rect x="122.5" y="228" width="110" height="8" rx="4"/>
                            <rect x="524.5" y="200" width="28" height="28"/><rect x="483.5" y="228" width="110" height="8" rx="4"/>
                          </g>
                          <g clip-path="url(#rig-types-static)">
                            <image class="rig-photo" href="/photos/hero-beach.jpg" x="0" y="0" width="716" height="200" preserveAspectRatio="xMidYMid slice"/>
                            <rect class="rig-frame" x="0" y="0" width="355" height="200" rx="10"/>
                            <rect class="rig-frame" x="361" y="0" width="355" height="200" rx="10"/>
                          </g>
                        </svg>
                        <figcaption class="mt-4 text-sm text-cd-text-secondary">Photograph by <a class="cd-inline-link rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://unsplash.com/@gkumar2175">Ganapathy Kumar</a> on Unsplash.</figcaption>
                    </figure>
                    <div class="col-span-12 lg:col-span-4 lg:col-start-9 lg:row-start-1">
                        <p class="types-kind flex items-center gap-2.5 text-sm font-semibold text-cd-text-secondary">
                            <Icon name="image" class="size-5 shrink-0" />Static, one image
                        </p>
                        <h3 class="mt-3 text-xl font-semibold tracking-[-0.01em] text-cd-text">One photograph, every display</h3>
                        <p class="mt-4 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Drag a wide photograph into place on a canvas that mirrors your real display arrangement, and it goes up as one continuous picture. It stays there until you change it.</p>
                    </div>
                </div>
            </div>

        </div>
    </div>

</section>
