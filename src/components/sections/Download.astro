---
import Icon from '../Icon.astro'
---

<style>
  /* --- Download: requirements list ---
     A plain definition list, ruled rather than boxed, so it stays quiet. */
  .dl-row {
    display: grid;
    grid-template-columns: 7.5rem 1fr;
    gap: 0.25rem 1.5rem;
    border-top: 1px solid var(--color-cd-border);
    padding-block: 0.875rem;
  }

  @media (max-width: 24.9375rem) {
    .dl-row {
      grid-template-columns: 1fr;
    }
  }

  /* One wash of accent behind the buttons, falling off to nothing well before any
     edge. It reads as light on the closing section rather than as a gradient, and
     at a tenth of the accent it moves no type off its measured contrast. */
  #download {
    background-image: radial-gradient(
      70% 60% at 22% 36%,
      color-mix(in oklab, var(--color-cd-accent) 10%, transparent) 0%,
      transparent 72%
    );
  }

  .cd-code {
    font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace;
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--color-cd-text);
    overflow-x: auto;
  }
</style>

<section id="download" aria-labelledby="download-heading" class="cd-section bg-cd-canvas">
  <div class="cd-shell">
    <div class="grid grid-cols-12 gap-x-6 gap-y-12">
      <div class="col-span-12 lg:col-span-6" data-reveal>
        <p class="cd-eyebrow">Download</p>
        <h2 id="download-heading" class="mt-3 text-3xl font-bold leading-[1.05] tracking-[-0.025em] text-balance text-cd-text sm:text-4xl lg:text-5xl">Download SpreadPaper, <span class="text-cd-accent">free and open source</span></h2>
        <p class="mt-5 max-w-[46ch] text-lg leading-relaxed text-pretty text-cd-text-secondary sm:text-xl">SpreadPaper is a native macOS app released under the MIT licence. Every version ships from GitHub as a DMG and a ZIP, with no account to make and nothing to pay for.</p>

        <div class="mt-8 flex flex-wrap items-center gap-3">
          <a href="https://github.com/spreadpaper/SpreadPaper/releases/latest" class="cd-button cd-button-primary h-11 px-6 text-[0.9375rem] focus-visible:outline-2 focus-visible:outline-cd-accent focus-visible:outline-offset-2 ">
            <Icon name="download-simple" width="18" height="18" />
            Download the latest release
          </a>
          <a href="https://github.com/spreadpaper/SpreadPaper" class="cd-button cd-button-secondary h-11 px-6 text-[0.9375rem] focus-visible:outline-2 focus-visible:outline-cd-accent focus-visible:outline-offset-2 ">View the source</a>
        </div>
      </div>

      <div class="col-span-12 lg:col-span-5 lg:col-start-8" data-reveal>
        <h3 class="text-xl font-semibold tracking-[-0.01em] text-cd-text">What it needs</h3>
        <dl class="mt-5 border-b border-cd-border">
          <div class="dl-row">
            <dt class="text-sm text-cd-text-secondary">macOS</dt>
            <dd class="text-base leading-relaxed text-cd-text">15.0 Sequoia or later</dd>
          </div>
          <div class="dl-row">
            <dt class="text-sm text-cd-text-secondary">Mac</dt>
            <dd class="text-base leading-relaxed text-cd-text">Apple Silicon</dd>
          </div>
          <div class="dl-row">
            <dt class="text-sm text-cd-text-secondary">Displays</dt>
            <dd class="text-base leading-relaxed text-cd-text">One is enough for a Light and Dark pair or a day on a schedule. Two or more to carry one photograph across them</dd>
          </div>
          <div class="dl-row">
            <dt class="text-sm text-cd-text-secondary">Download</dt>
            <dd class="text-base leading-relaxed text-cd-text">DMG or ZIP, from the GitHub releases page</dd>
          </div>
          <div class="dl-row">
            <dt class="text-sm text-cd-text-secondary">Licence</dt>
            <dd class="text-base leading-relaxed text-cd-text">MIT, free and open source</dd>
          </div>
        </dl>
        <p class="mt-5 text-sm text-cd-text-secondary">On an Intel Mac or an older macOS? <a class="cd-inline-link focus-visible:outline-2 focus-visible:outline-cd-accent focus-visible:outline-offset-2 " href="https://github.com/spreadpaper/SpreadPaper/issues">Open a feature request</a> and say so.</p>
      </div>

      <div class="col-span-12 border-t border-cd-border pt-12" data-reveal>
        <h3 class="text-xl font-semibold tracking-[-0.01em] text-cd-text">Apple could not verify SpreadPaper is free of malware</h3>
        <p class="mt-3 max-w-[70ch] text-base leading-relaxed text-pretty text-cd-text">That is the wording macOS uses for any app not signed with a paid Apple Developer ID. Nothing has been found in SpreadPaper, and nothing has been scanned: the check is for a signature, not for malware. You do not have to take that on trust, because the <a class="cd-inline-link focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://github.com/spreadpaper/SpreadPaper">source is public</a> and every release is <a class="cd-inline-link focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" href="https://github.com/spreadpaper/SpreadPaper/blob/main/.github/workflows/release.yml">built from it by GitHub Actions</a>.</p>
        <p class="mt-4 max-w-[70ch] text-base leading-relaxed text-pretty text-cd-text">Take either route below. It takes a minute, and only the first time.</p>

        <div class="mt-10 grid gap-x-10 gap-y-10 md:grid-cols-2">
          <div>
            <h4 class="text-base font-semibold text-cd-text">Through System Settings</h4>
            <ol class="mt-4 flex max-w-[52ch] list-decimal flex-col gap-2.5 pl-5 text-base leading-relaxed text-pretty text-cd-text-secondary marker:text-cd-text-secondary">
              <li>Drag SpreadPaper to your Applications folder.</li>
              <li>Open it, then click Done on the warning.</li>
              <li>Open System Settings, then Privacy and Security, and scroll to the bottom.</li>
              <li>Click Open Anyway, then confirm with Open.</li>
            </ol>
          </div>

          <div>
            <h4 class="text-base font-semibold text-cd-text">Or in Terminal, before you open it</h4>
            <p class="mt-4 max-w-[52ch] text-base leading-relaxed text-pretty text-cd-text-secondary">Drag SpreadPaper to your Applications folder, then run this. It skips the warning entirely.</p>
            <div class="mt-4 flex max-w-[52ch] items-start gap-2 rounded-xl border border-cd-border bg-cd-bg p-3">
              <code id="quarantine-command" class="cd-code min-w-0 flex-1 py-1 pl-1">xattr -dr com.apple.quarantine /Applications/SpreadPaper.app</code>
              <button type="button" hidden data-copy-target="quarantine-command" data-copy-label="Copy" data-copied-label="Copied" class="cd-button cd-button-secondary h-9 shrink-0 gap-1.5 px-3 text-xs focus-visible:outline-2 focus-visible:outline-cd-accent focus-visible:outline-offset-2 "><Icon name="copy" data-copy-icon="idle" class="h-3.5 w-3.5" /><Icon name="check" data-copy-icon="done" hidden class="h-3.5 w-3.5" /><span data-copy-text aria-live="polite">Copy</span><span class="sr-only"> the quarantine command</span></button>
            </div>
          </div>
        </div>

        <p class="mt-10 max-w-[70ch] text-sm leading-relaxed text-pretty text-cd-text-secondary">The check is called Gatekeeper. Signing an app to satisfy it costs 99 dollars a year, which is not a cost this project carries.</p>
      </div>
    </div>
  </div>
</section>
