---
import Icon from '../Icon.astro'
---

<!-- The editor: placing one photograph exactly where the monitors are. -->
<section id="editor" class="cd-section bg-cd-bg" aria-labelledby="editor-title">
  <style>
    /* The bezel demonstration. The slider hands over a width in points, the rig wants viewBox
       units, and the conversion lives here: a 27-inch display is 2560 points across the 355
       units the generator draws it at, which puts one point at 0.139 of a unit. Recompute
       this if `SCREEN.monitor27` in scripts/rigs.mjs ever changes. The frame grows into the
       screen it sits on, which is the whole idea, since the photograph behind it never moves
       and the strip the housing covers is the part of the picture you agree not to see. */
    #editor .ed-bezel { --ed-bezel: 60; }
    #editor .ed-bezel .rig { --rig-bezel: calc(var(--ed-bezel) * 0.139); }
    #editor .ed-bezel .rig-frame { transition: stroke-width 120ms ease-out; }

    /* The control does nothing without the script that binds it, so it ships hidden and the
       script unhides it. The rule has to be restated: a display utility on the same element
       outranks the browser's own [hidden] rule and would show it anyway. */
    #editor [data-bezel-control][hidden] { display: none; }

    #editor .ed-range {
      appearance: none;
      -webkit-appearance: none;
      width: 100%;
      max-width: 22rem;
      height: 2.75rem;
      background: transparent;
      border-radius: 6px;
    }

    #editor .ed-range::-webkit-slider-runnable-track {
      height: 6px;
      border-radius: 999px;
      background: var(--color-cd-bg-elevated);
      border: 1px solid var(--color-cd-border-strong);
    }

    #editor .ed-range::-moz-range-track {
      height: 6px;
      border-radius: 999px;
      background: var(--color-cd-bg-elevated);
      border: 1px solid var(--color-cd-border-strong);
    }

    #editor .ed-range::-webkit-slider-thumb {
      appearance: none;
      -webkit-appearance: none;
      width: 20px;
      height: 20px;
      margin-top: -8px;
      border: none;
      border-radius: 999px;
      background: var(--color-cd-accent);
    }

    #editor .ed-range::-moz-range-thumb {
      width: 20px;
      height: 20px;
      border: none;
      border-radius: 999px;
      background: var(--color-cd-accent);
    }

    @media (prefers-reduced-motion: reduce) {
      #editor .ed-bezel .rig-frame { transition: none; }
    }
  </style>

  <div class="cd-shell">
    <div class="grid grid-cols-12 gap-x-6 gap-y-6">
      <div class="col-span-12 lg:col-span-6" data-reveal>
        <p class="cd-eyebrow">The editor</p>
        <h2 id="editor-title" class="mt-4 text-3xl sm:text-4xl lg:text-5xl font-bold tracking-[-0.025em] leading-[1.05] text-cd-text text-balance">Place the wallpaper exactly where your monitors sit</h2>
      </div>
      <div class="col-span-12 lg:col-span-5 lg:col-start-8 lg:self-end" data-reveal>
        <p class="text-lg sm:text-xl leading-relaxed text-cd-text-secondary max-w-[46ch] text-pretty">The canvas is your display arrangement, the one macOS already knows about: the same sizes, the same order, the same gaps. Drop a photograph on it and drag, and it snaps to the middle and to the edges as it passes them, so what you line up here is what lands on the glass.</p>
      </div>
    </div>

    <div class="mt-16 grid grid-cols-12 gap-x-6 gap-y-10 lg:items-center">
      <figure class="col-span-12 lg:col-span-7" data-reveal>
        <!-- Generated by `markup('mixed')` from scripts/rigs.mjs, hand edits and all now in
             the generator, including the four glyphs `EditorView.hudIconButton` really draws. -->
        <svg class="rig rig-mixed" viewBox="0 0 728 403" role="img" aria-label="The editor canvas: a snow covered mountain ridge, with an ultrawide display and one stood on its end outlined over it and the photograph reaching past both." focusable="false">
          <defs>
            <clipPath id="rig-editor-canvas">
              <rect x="24" y="100" width="474" height="203" rx="10"/>
              <rect x="504" y="24" width="200" height="355" rx="10"/>
            </clipPath>
          </defs>
          <image class="rig-bleed" href="/photos/1200/hero-day-2.jpg" x="0" y="0" width="728" height="403" preserveAspectRatio="xMidYMid slice"/>
          <rect class="rig-crop" x="14" y="14" width="700" height="375" rx="8"/>
          <g clip-path="url(#rig-editor-canvas)">
            <image class="rig-photo" href="/photos/1200/hero-day-2.jpg" x="0" y="0" width="728" height="403" preserveAspectRatio="xMidYMid slice"/>
            <rect class="rig-frame" x="24" y="100" width="474" height="203" rx="10"/>
            <rect class="rig-frame" x="504" y="24" width="200" height="355" rx="10"/>
          </g>
          <g class="rig-hud">
            <rect class="rig-hud-bar" x="175" y="253" width="172" height="30" rx="15"/>
            <Icon name="minus" x="191" y="261" width="14" height="14" />
            <Icon name="plus" x="233" y="261" width="14" height="14" />
            <Icon name="arrows-out-simple" x="275" y="261" width="14" height="14" />
            <Icon name="arrows-left-right" x="317" y="261" width="14" height="14" />
          </g>
        </svg>
        <figcaption class="mt-4 text-sm leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">An ultrawide with a display stood on its end beside it, laid out the way the Mac reports them, and the editor's own controls floating over the bottom of the canvas. The hairline rectangle is what the render keeps, and the photograph reaching past it is cropped away.</figcaption>
      </figure>

      <div class="col-span-12 lg:col-span-4 lg:col-start-9 space-y-8" data-reveal>
        <div>
          <h3 class="text-xl font-semibold tracking-[-0.01em] text-cd-text">Zoom, fit and flip</h3>
          <p class="mt-2 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">A small set of controls floats over the bottom of the canvas: zoom between 10 and 300 per cent, fit the image to the canvas in one click, and mirror it horizontally when the composition reads better the other way round.</p>
        </div>
        <div>
          <h3 class="text-xl font-semibold tracking-[-0.01em] text-cd-text">One file for every screen</h3>
          <p class="mt-2 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Each display gets its own rendered file, keyed on the display ID rather than the name, so two identical monitors never end up sharing one picture. Wallpapers go up one at a time, so a second click cannot overtake the first.</p>
        </div>
        <div>
          <h3 class="text-xl font-semibold tracking-[-0.01em] text-cd-text">Look before you save</h3>
          <p class="mt-2 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Preview puts the wallpaper on your screens with the editor still open, so you can judge it where it will live. Save keeps it as a preset, and Save &amp; Apply does both at once.</p>
        </div>
      </div>
    </div>

    <hr class="mt-16 border-t border-cd-border">

    <div class="mt-16 grid grid-cols-12 gap-x-6 gap-y-10">
      <div class="col-span-12 lg:col-span-4" data-reveal>
        <h3 class="text-xl font-semibold tracking-[-0.01em] text-cd-text">Bezel compensation</h3>
        <p class="mt-3 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Monitors have frames, and on a spread wallpaper those frames sit right in the middle of the picture. SpreadPaper pushes the display frames apart by the width of the housing between them, so the photograph carries on behind each frame rather than being drawn on both screens at once. A ridgeline then runs straight across the gap.</p>
        <p class="mt-4 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">That is a trade you are making on purpose: the strip behind each frame is part of the image you never see. Set one width for all your displays or a pair for each of them, horizontal for the left and right edges, vertical for the top and bottom, measured in points. The controls appear once a second display is connected, and Settings holds the gap new displays start from.</p>
      </div>

      <figure class="col-span-12 lg:col-span-7 lg:col-start-6" data-reveal>
        <div class="ed-bezel" id="bezel-rig">
          <svg class="rig rig-trio rig-glass hidden sm:block" style="--rig-glow-color: rgb(94 92 230 / 0.10)" viewBox="0 0 1077 236" role="img" aria-label="One alpine ridge at sunrise carried across three matched monitors, the frames drawn as glass so the picture can be seen carrying on behind them." focusable="false">
            <defs>
              <clipPath id="rig-editor-bezel-three">
                <rect x="0" y="0" width="355" height="200" rx="10"/>
                <rect x="361" y="0" width="355" height="200" rx="10"/>
                <rect x="722" y="0" width="355" height="200" rx="10"/>
              </clipPath>
            </defs>
            <ellipse class="rig-glow" cx="538.5" cy="232" rx="474" ry="18"/>
            <g class="rig-stand">
              <rect x="163.5" y="200" width="28" height="28"/><rect x="122.5" y="228" width="110" height="8" rx="4"/>
              <rect x="524.5" y="200" width="28" height="28"/><rect x="483.5" y="228" width="110" height="8" rx="4"/>
              <rect x="885.5" y="200" width="28" height="28"/><rect x="844.5" y="228" width="110" height="8" rx="4"/>
            </g>
            <g clip-path="url(#rig-editor-bezel-three)">
              <image class="rig-photo" href="/photos/1200/hero-day-1.jpg" x="0" y="0" width="1077" height="200" preserveAspectRatio="xMidYMid slice"/>
              <rect class="rig-frame" x="0" y="0" width="355" height="200" rx="10"/>
              <rect class="rig-frame" x="361" y="0" width="355" height="200" rx="10"/>
              <rect class="rig-frame" x="722" y="0" width="355" height="200" rx="10"/>
            </g>
          </svg>

          <svg class="rig rig-dual rig-glass sm:hidden" style="--rig-glow-color: rgb(94 92 230 / 0.10)" viewBox="0 0 716 236" role="img" aria-label="One alpine ridge at sunrise carried across two matched monitors, the frames drawn as glass so the picture can be seen carrying on behind them." focusable="false">
            <defs>
              <clipPath id="rig-editor-bezel-two">
                <rect x="0" y="0" width="355" height="200" rx="10"/>
                <rect x="361" y="0" width="355" height="200" rx="10"/>
              </clipPath>
            </defs>
            <ellipse class="rig-glow" cx="358" cy="232" rx="315" ry="18"/>
            <g class="rig-stand">
              <rect x="163.5" y="200" width="28" height="28"/><rect x="122.5" y="228" width="110" height="8" rx="4"/>
              <rect x="524.5" y="200" width="28" height="28"/><rect x="483.5" y="228" width="110" height="8" rx="4"/>
            </g>
            <g clip-path="url(#rig-editor-bezel-two)">
              <image class="rig-photo" href="/photos/1200/hero-day-1.jpg" x="0" y="0" width="716" height="200" preserveAspectRatio="xMidYMid slice"/>
              <rect class="rig-frame" x="0" y="0" width="355" height="200" rx="10"/>
              <rect class="rig-frame" x="361" y="0" width="355" height="200" rx="10"/>
            </g>
          </svg>
        </div>

        <!-- Everything this block says is true only once the script has bound the slider,
             which is why the sentence about dragging lives here and not in the caption. -->
        <div class="mt-6" data-bezel-control hidden>
          <div class="flex flex-wrap items-center gap-x-5 gap-y-1">
            <label class="text-sm font-semibold text-cd-text" for="bezel-width">Frame width, one edge</label>
            <input class="ed-range grow rounded-md focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cd-accent" id="bezel-width" type="range" min="0" max="150" step="5" value="60" data-bezel-slider data-bezel-target="bezel-rig" data-bezel-prop="--ed-bezel" data-bezel-unit=" pt" data-bezel-unit-label="points">
            <output class="w-14 text-sm font-semibold tabular-nums text-cd-text" for="bezel-width" data-bezel-readout>60 pt</output>
          </div>
          <p class="mt-2 text-sm leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Drag it and the frames widen while the photograph stays where it is, so the hidden strip grows and the ridgeline still meets on the other side. The range is 0 to 150 points, which is what the app gives one edge of one display.</p>
        </div>

        <figcaption class="mt-4 text-sm leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Matched displays with about an inch of housing between each pair of screens. The frames are drawn as glass, so you can see the picture carrying on behind them, with a blank line down the middle of each gap where the air between two monitors is. What the glass covers is the part of the picture the render hides on purpose, and the ridgeline meets at the same height on the far side of it. Whatever width the housing is, the output is correct for the monitors it describes.</figcaption>
      </figure>
    </div>
  </div>
</section>
