---
import Icon from '../Icon.astro'
---

<!-- The gallery: living with the wallpapers you have already made. -->
<section id="gallery" class="cd-section bg-cd-bg-secondary" aria-labelledby="gallery-title">
  <style>
    /* Lifts the preset cards off the gallery's own background. Same card, one surface up.
       The border answers a pointer because the app's own cards do, and a card that looks
       clickable and stays inert reads worse than one that plainly is not. Border only: no
       lift, no shadow, no scale. */
    #gallery .gl-card {
      background-color: var(--color-cd-bg-elevated);
      transition: border-color 150ms ease-out;
    }

    #gallery .gl-card:hover { border-color: var(--color-cd-border-strong); }

    @media (prefers-reduced-motion: reduce) {
      #gallery .gl-card { transition: none; }
    }

    /* All four thumbnails come from scripts/rigs.mjs: `thumb`, `thumb-pair`, `thumb-portrait`
       and `thumb-laptop`, one arrangement per preset.

       One tile shape for every thumbnail, whatever is drawn inside it. The four presets are
       four different setups, so their rigs are different shapes, and each is held to its own
       size inside a tile they all share, the way a row of presets sits in the app. A rig with
       more screens is physically wider, so it lands smaller in the tile, which is honest. The
       aspect belongs to the tile and never to a rig, which carries its own in its viewBox. */
    #gallery .gl-tile {
      display: flex;
      align-items: center;
      justify-content: center;
      aspect-ratio: 16 / 9;
      padding: 1rem;
      border-radius: 10px;
      background-color: var(--color-cd-canvas);
    }

    #gallery .gl-tile .rig {
      width: auto;
      height: auto;
      max-width: 100%;
      max-height: 100%;
    }
  </style>

  <div class="cd-shell">
    <div class="grid grid-cols-12 gap-x-6 gap-y-6">
      <div class="col-span-12 lg:col-span-5" data-reveal>
        <p class="cd-eyebrow">The gallery</p>
        <h2 id="gallery-title" class="mt-4 text-3xl sm:text-4xl lg:text-5xl font-bold tracking-[-0.025em] leading-[1.05] text-cd-text text-balance">Every wallpaper you make stays one click away</h2>
      </div>
      <div class="col-span-12 lg:col-span-6 lg:col-start-7 lg:self-end" data-reveal>
        <p class="text-lg sm:text-xl leading-relaxed text-cd-text-secondary max-w-[46ch] text-pretty">Saving writes a preset: the image, where you put it, the zoom, the flip, and the times if it runs on a schedule. The gallery lists them all with a thumbnail, and marks the one that is on your screens right now.</p>
        <p class="mt-4 text-base leading-relaxed text-cd-text-secondary max-w-[62ch] text-pretty">Narrow the list to one kind from the sidebar, or search it by name. Every card carries a menu that renames a preset, duplicates it, shows its image in Finder, or deletes it and leaves the image exactly where it was.</p>
      </div>
    </div>

    <ul role="list" class="mt-16 grid grid-cols-12 gap-x-6 gap-y-8">
      <li class="col-span-12 sm:col-span-6" data-reveal>
        <article class="cd-card gl-card p-3">
          <div class="relative">
            <div class="gl-tile">
              <svg class="rig rig-thumb" viewBox="0 0 346 64" aria-hidden="true" focusable="false">
                <defs>
                  <clipPath id="rig-gallery-1">
                    <rect x="0" y="0" width="114" height="64" rx="3"/>
                    <rect x="116" y="0" width="114" height="64" rx="3"/>
                    <rect x="232" y="0" width="114" height="64" rx="3"/>
                  </clipPath>
                </defs>
                <g clip-path="url(#rig-gallery-1)">
                  <image class="rig-photo" href="/photos/hero-beach.jpg" x="0" y="0" width="346" height="64" preserveAspectRatio="xMidYMid slice"/>
                  <rect class="rig-frame" x="0" y="0" width="114" height="64" rx="3"/>
                  <rect class="rig-frame" x="116" y="0" width="114" height="64" rx="3"/>
                  <rect class="rig-frame" x="232" y="0" width="114" height="64" rx="3"/>
                </g>
              </svg>
            </div>
            <p class="absolute top-2 left-2 flex items-center gap-1.5 rounded-full border border-cd-border bg-cd-canvas/85 px-2.5 py-1 text-sm font-semibold text-cd-text">
              <span class="block size-1.5 rounded-full bg-cd-success" aria-hidden="true"></span>
              Applied
            </p>
          </div>
          <div class="mt-3">
            <h3 class="truncate text-base font-semibold text-cd-text">Maui sunset</h3>
            <span class="mt-1.5 inline-flex items-center gap-1.5 rounded-md border border-cd-border bg-cd-bg-secondary px-2 py-1 text-sm text-cd-text-secondary">
              <Icon name="image" class="size-3.5 text-cd-text-secondary" />
              Static
            </span>
          </div>
        </article>
      </li>

      <li class="col-span-12 sm:col-span-6" data-reveal>
        <article class="cd-card gl-card p-3">
          <div class="gl-tile">
            <svg class="rig rig-thumb-pair" viewBox="0 0 230 64" aria-hidden="true" focusable="false">
              <defs>
                <clipPath id="rig-gallery-2">
                  <rect x="0" y="0" width="114" height="64" rx="3"/>
                  <rect x="116" y="0" width="114" height="64" rx="3"/>
                </clipPath>
              </defs>
              <g clip-path="url(#rig-gallery-2)">
                <image class="rig-photo" href="/photos/1200/hero-beach-night.jpg" x="0" y="0" width="230" height="64" preserveAspectRatio="xMidYMid slice"/>
                <rect class="rig-frame" x="0" y="0" width="114" height="64" rx="3"/>
                <rect class="rig-frame" x="116" y="0" width="114" height="64" rx="3"/>
              </g>
            </svg>
          </div>
          <div class="mt-3">
            <h3 class="truncate text-base font-semibold text-cd-text">Beach, day and night</h3>
            <span class="mt-1.5 inline-flex items-center gap-1.5 rounded-md border border-cd-border bg-cd-bg-secondary px-2 py-1 text-sm text-cd-text-secondary">
              <Icon name="circle-half" class="size-3.5 text-cd-appearance" />
              Light &amp; Dark
            </span>
          </div>
        </article>
      </li>

      <li class="col-span-12 sm:col-span-6" data-reveal>
        <article class="cd-card gl-card p-3">
          <div class="gl-tile">
            <svg class="rig rig-thumb-laptop" viewBox="0 0 174 70" aria-hidden="true" focusable="false">
              <defs>
                <clipPath id="rig-gallery-3">
                  <rect x="0" y="0" width="114" height="64" rx="3"/>
                  <rect x="116" y="32" width="58" height="37" rx="2"/>
                </clipPath>
              </defs>
              <g clip-path="url(#rig-gallery-3)">
                <image class="rig-photo" href="/photos/1200/hero-day-3.jpg" x="0" y="0" width="174" height="70" preserveAspectRatio="xMidYMid slice"/>
                <rect class="rig-frame" x="0" y="0" width="114" height="64" rx="3"/>
                <rect class="rig-frame" x="116" y="32" width="58" height="37" rx="2"/>
              </g>
            </svg>
          </div>
          <div class="mt-3">
            <h3 class="truncate text-base font-semibold text-cd-text">A day outside</h3>
            <span class="mt-1.5 inline-flex items-center gap-1.5 rounded-md border border-cd-border bg-cd-bg-secondary px-2 py-1 text-sm text-cd-text-secondary">
              <Icon name="clock" class="size-3.5 text-cd-dynamic" />
              Dynamic
            </span>
          </div>
        </article>
      </li>

      <li class="col-span-12 sm:col-span-6" data-reveal>
        <article class="cd-card gl-card p-3">
          <div class="gl-tile">
            <svg class="rig rig-thumb-portrait" viewBox="0 0 180 114" aria-hidden="true" focusable="false">
              <defs>
                <clipPath id="rig-gallery-4">
                  <rect x="0" y="0" width="64" height="114" rx="3"/>
                  <rect x="66" y="50" width="114" height="64" rx="3"/>
                </clipPath>
              </defs>
              <g clip-path="url(#rig-gallery-4)">
                <image class="rig-photo" href="/photos/1200/hero-day-4.jpg" x="0" y="0" width="180" height="114" preserveAspectRatio="xMidYMid slice"/>
                <rect class="rig-frame" x="0" y="0" width="64" height="114" rx="3"/>
                <rect class="rig-frame" x="66" y="50" width="114" height="64" rx="3"/>
              </g>
            </svg>
          </div>
          <div class="mt-3">
            <h3 class="truncate text-base font-semibold text-cd-text">Night ridge</h3>
            <span class="mt-1.5 inline-flex items-center gap-1.5 rounded-md border border-cd-border bg-cd-bg-secondary px-2 py-1 text-sm text-cd-text-secondary">
              <Icon name="image" class="size-3.5 text-cd-text-secondary" />
              Static
            </span>
          </div>
        </article>
      </li>
    </ul>
  </div>
</section>
